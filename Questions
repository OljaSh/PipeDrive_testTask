
-----------------NEXT------------------------------
- селекторы для SignUpPage textWarningAddPassword/textWarningAddCompanyIndustry

- css selectors - пока не понятно ( http://flukeout.github.io/# )

-  под FireFox не работает выбор языка
- на Android в портретной ориентации экрана не проходит assert по и мени пользователя т.к. имени не видно
  как поступить в этом слечае?

- iOS на эмуляторе запускает  Safari, но передает адрес 127.0.0.1 ( с запуском на реальном устройстве еще не разобралась )
    решение Thread.sleep - но на проекте пока не работает

- бат файлы и json для запуска grid

- chrome  не разворачтвается на весь экран

---------------------------------------------------

- Как получить CSS селекторы
   пример, LoginPage private By labelEmail = By.cssSelector("label[for='login']")

-  Как проверить URL страницы ( для assert )

- Sign Up Page как лучше сделать селекторы для ошибок, при запуске тестов не находится

- LoginWithRegisteredUserAllErrorsTest  в Allure report : Skipped due to dependency on other skipped or failed test methods
  ( тест loginWithEmptyEmailOrPassword падает - ожидаемо )

------------------------------------------------------
- Настройка проверки на мобилках ( локально настроен appium, iOS/Android эмуляторы, могу подключить реальные девайсы )

- Debug

- можно ли и  нужно ли задавать в DataProvider поля предефайненые в sourse ?

- DataProviders разное описание -> разобраться
  return Stream.of( )
  ollections.singletonList(new RegisteredUser("shseven@hotmail.com", "TestTest")).stream()




--------------------------
---------------------------
1. На  LoginPage одна и та же кнопка Login при разных сценариях ведет на разные страницы.
   Сейчас она у меня рассписана как 2 разные кнопки с разным поведением
   Насколько такой подход правильный?

2. При первом запуске сайт всегда запускает на эстонском независимо от локали компа или какой url задан
   Поэтому перед каждым тестом у меня принудительный вызов языка.
   Насколько это правильное решение?

3.  В рамках одного тестового файла  кешь бразера не чиститься, поэтому если сделать login - Log Out,
    и снова ввсети URL то показывается welcomeback  страницу и тесты падают т.к. у меню переклчения языка другой локатор

    Как правильно организовать тесты в этом случае?
    Сейсас у меня выставлен принудительный приоритет и  положительный тест запускается в конце.

4. В java.util  нет поддержки locale.ESTONIAN
   ЧТо делать?


5. Локализация!!!!

-------------------------

   - можно ли хранить один DataProvider для различных тестовых класов ?


8.  Передача локали в виде параметра

9. Кодировки expected [SalasÃµna] but found [Salasõna]
    русский expected [ÐÐ¾Ð¹ÑÐ¸] but found [Logi sisse]


- В позитивном тест кейсе поменять захардкоденное имя пользователя что бы считывалось автоматическ

 - как лучше задать данные по environment, что бы генерились автоматически
 https://github.com/allure-framework/allure-core/wiki/Environment              - надо менять код  Allure

 -в списке в  title отображается название тест метода, а не описание анотации  - смотреть 15 видео урок

 - title пофейленых тестов нечитаем @Title - бага Allure ( пофикшена )

- Safari и IE не понимает половину локаторов
Session ID: null
*** Element info: {Using=xpath, value=.//*[@id='login_form']/div[1]/h1}
	at com.pipedrive.testcases.LoginWithRegisteredUser.LoginPageElementVisibilityTest.checkLabelsOnLoginPage(LoginPageElementVisibilityTest.java:28)

     Под Safari лучше не проверять
     Для IE поставить последнюю версию

- Создание пользователя, как лучше  организовтаь проверку негативных сценариев? (пустые/длинные/невалидные поля) так же через DataProvider?
     Через DataProviders

- как сделать что бы при запуске тестов окно браузера не открывалось в foreground
     Невозможно

- LoginPageElementVisibilityTest Тест падает и не идет дальше если хотя бы один локатор не совпадает
    Тест должен продолжиться,
       - Решается через SoftAssert - смотреть 15 видео урок

- В позитивном тест кейсе поменять захардкоденное имя пользователя что бы считывалось автоматически
    -  сделала сама


-----------------NEXT------------------------------

- Как получить CSS селекторы
   пример, LoginPage private By labelEmail = By.cssSelector("label[for='login']")

-  Как проверить URL страницы ( для assert )

------------------------------------------------------
- Настройка проверки на мобилках


- Как лучше настроить проверку локалей? Через  Suite?

- Debug

- можно ли и  нужно ли задавать в DataProvider поля предефайненые в sourse ?

- DataProviders разное описание -> разобраться
